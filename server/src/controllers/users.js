const users_db = require('../db/threads.js');

async function get_users(req, res) {
    // const results = await threads_db.get_threads();
    // res.send(results);
}

async function get_user(req, res) {
    // const results = await threads_db.get_threads();
    // res.send(results);
}

async function get_user_groups(req, res) {
    // const results = await threads_db.get_threads();
    // res.send(results);
}

async function get_users_posts(req, res) {
    // const results = await threads_db.get_threads();
    // res.send(results);
}

module.exports = {
    // get_threads,
    // get_thread,
    // get_comments_from_thread,
    // create_thread,
    // update_thread,
    // delete_thread
};